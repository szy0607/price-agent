# 用户注册与登录 — 接口契约与技术方案

> 版本：v1.0（草案，待评审） ｜ 日期：2026-09-13
> 归属：`开发流程.md` §3 第②步「设计先行」的产物 → 评审通过后再写代码
> 本轮范围：增量推进（注册 → 登录 → 登出 → 当前用户），实现到哪一步都行
>
> ⚠️ **2026-09-23 修订（§2 接口契约已按 `app/` 现有实现回写）**：
> 信封字段改 **`msg`**、成功文案 **`"success"`**、请求体改 **`user_email` + `username`**、时间字段改 **`create_time`**。
> 依据仍是项目约定「**契约可以灵活变通，但改动后果必须同步告知**」——
> **契约跟随代码，不反过来绑架代码**（同 §1.1）。
> 📌 分区判据见新增的 **§7.3**：哪些无脑跟代码、哪些**必须先说影响、由大宋拍板**。

---

## 0. 本轮已定决策

| 项 | 结论 |
|----|------|
| 注册 / 登录凭据 | **邮箱 + 密码** |
| 数据存储 | **MySQL 8**（业务库，SQLAlchemy 2.x **异步** + aiomysql 驱动）＋ **Milvus**（RAG 向量库，M3 启用） |
| 登录态 | **Session Cookie**（服务端会话，session 落 MySQL 表） |
| 后端 | FastAPI（`开发流程.md` §6） |
| 范围 | 增量推进，不锁死本轮终点 |

> 📌 技术栈决策（替代设计稿 §5 原定的 `PostgreSQL + pgvector`）：**业务库 = MySQL 8，向量库 = Milvus**。Milvus 属 M3 范围，**本轮注册 / 登录不涉及**；双库分工见 §8。

---

## 1. 数据库设计（MySQL 8，utf8mb4）

### 1.1 `users`

> ⚠️ **2026-09-21 修订（以代码为准）**：大宋拍板——**保留自己的命名**（`user_email` / `create_time` / `last_login_time`）、
> **加回 `username`**、**主键用 `INT` 不升 `BIGINT`**、**时间精度用秒级不升毫秒**、**`username` 不唯一**。
> 依据：项目约定「**契约可以灵活变通，但改动后果必须同步告知**」——**契约跟随代码，不反过来绑架代码**。

```sql
CREATE TABLE users (
  id              INT             NOT NULL AUTO_INCREMENT,
  user_email      VARCHAR(255)    NOT NULL                COMMENT '规范化后小写；登录凭据',
  username        VARCHAR(100)    NOT NULL                COMMENT '展示名，允许重名',
  password_hash   VARCHAR(255)    NOT NULL                COMMENT 'bcrypt/argon2，禁明文',
  create_time     DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  last_login_time DATETIME        NULL,
  PRIMARY KEY (id),
  UNIQUE KEY uk_users_email (user_email)
) ENGINE=InnoDB;
```

> **不写 `CHARSET` / `COLLATE` / `ENGINE` 之外的方言细节** —— 实测库 `price_agent` 建库时就是
> `DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci`，**表会继承，不用重复声明**。
> 这才是"灵活变通"该有的样子：**能不写就不写，前提是实测过默认值是对的**（本契约此前过度指定了，已删）。

要点：
- **只存哈希**，绝不存明文密码（红线）。
- `user_email` 入库前统一 `lower().strip()`；唯一索引兜底并发注册。
- **6 个字段就是全部**——没有 `nickname` / `status` / `updated_at` / `deleted_at`（见下 §1.1.1）。

#### 1.1.2 本轮"初步能跑"的宽松选择（**后果已实测，知情即可**）

| 选择 | 当前值 | 什么时候会咬你 | 到时怎么改 |
|---|---|---|---|
| 主键类型 | `INT` | 上限 **21.4 亿**，本项目基本撞不到 | 改列类型 = **`INPLACE`**（不重建表，但会动索引）。趁数据少时改便宜 |
| 时间精度 | `DATETIME`（**秒级**） | 同一秒内多条记录排序无法区分；并发写入的 `create_time` 会撞同一秒。**对注册/登录完全够用** | 加精度 = 改列类型 = `INPLACE` |
| `username` | **不唯一** | 允许重名 → **不能用它当登录凭据**（无法唯一定位人）。登录唯一键只能是 `user_email` | 想改唯一：`CREATE UNIQUE INDEX` = **`INPLACE`**，且存量重名要先清 |
| `user_email` 长度 | `VARCHAR(255)`（**2026-09-23 按代码回写**，原写 100） | 原担心：RFC 5321 允许 **254**，本机 `sql_mode` 含 **`STRICT_TRANS_TABLES`**（实测）→ 超长邮箱不是静默截断而是直接报错 → 注册 500。**改 255 后该风险已解除** | 无需再改 |

> **判据升级版（可复用）**：上一轮用「**时间点事件 vs 当前状态**」决定**要不要加这个字段**。
> 这一轮问题不同——字段已经有了，问的是「**给多大 / 多严**」。这时看的是：
> **改它要付什么代价**。走 `INSTANT` 的（加列）→ 随意；走 `INPLACE` 的（改类型 / 改索引）→ **能一次到位就别留尾巴**。
> 本轮这四项都是 `INPLACE`，所以**现在拍板、记进契约**，比以后改便宜。

#### 1.1.3 `username` 保留的两个注意点

| 注意点 | 说明 |
|---|---|
| **① 它不能当登录凭据** | 因为不唯一。登录唯一键是 `user_email`。**这条要在代码里写死**，否则以后一定有人写出 `WHERE username = ?` 然后拿错人 |
| **② 注册少了一条失败路径** | 只剩「邮箱冲突」一种 409 —— 比上一版更干净，**少一个用户枚举面**。这是"不加唯一约束"的额外收益 |

#### 1.1.1 字段取舍的判据（2026-09-20 修订）

原设计有 9 个字段，其中 4 个**没有需求来源**，已删除。删除依据是一条可复用的判据：

> **这个字段记的是「时间点事件」还是「当前状态」？**
>
> - **时间点事件** → 必须现在就有。**漏掉就永久丢失**，事后补只能得 `NULL`。
> - **当前状态** → 需要时再加。MySQL 8 加列走 `ALGORITHM=INSTANT`，**不重建表**（实测 2 行存量表瞬时完成，存量行自动回填默认值）。

| 字段 | 类型 | 判定 | 决定 |
|---|---|---|---|
| `id` `user_email` `password_hash` | — | 不成立功能就没有 | **保留** |
| `create_time` | 时间点事件 | 注册时刻补不回来 | **保留** |
| `last_login_time` | 时间点事件 | 登录历史补不回来；后续风控数据源 | **保留** |
| `username` | — | 2026-09-20 删过、**2026-09-21 加回**（大宋决定） | **保留**（后果见 §1.1.2） |
| ~~`nickname`~~ | 当前状态 | 无需求来源（惯例产物）；后补默认 `''` 语义正确 | **删** |
| ~~`updated_at`~~ | 行级变更时间 | **不是业务事件**，价值低；后补默认 `NULL` 也算对 | **删** |
| ~~`status`~~ | 当前状态 | 本轮无封号需求；后补默认 `1`（正常）语义正确 | **删** |
| ~~`deleted_at`~~ | 当前状态 | 本轮无注销需求；后补默认 `NULL`（未删）语义正确 | **删** |

> 📌 **命名口径**：本表字段名以**代码为准**（`user_email` / `create_time` / `last_login_time`），
> 不采用更常见的 `email` / `created_at` / `last_login_at`。**理由是大简单：代码已经这么写了，改命名要动 import 和已写好的查询。**
> **代价**：以后前端字段名、接口文档、日志字段都要跟着用这套名字，别混用两种风格。

**更正一处此前的错误说法**：本契约早先写「字段现在不加，以后要停机迁移」——**对加列而言不成立**。实测 MySQL 8.0.46，加列走 INSTANT、不重建表。真正有成本的是**加索引**和**改列类型**（两者为 INPLACE）。

**删除的连带影响**（施工时必须同步）：

| 被删字段 | 连带改动 |
|---|---|
| `nickname` | `RegisterReq` 少一个可选参数；`UserResp` 少一个字段；`register()` 少一个入参 |
| `status` | `login()` 里不再有「账号停用」分支 |
| `deleted_at` | **`_get_active_user()` 里的 `deleted_at IS NULL` 条件必须去掉**，否则 `AttributeError` |

**关于 PIPL 可删除权**：删掉 `deleted_at` **≠ 放弃红线⑥**（`开发流程.md` §7 第 6 条「用户可删除」），三点澄清：

1. **红线⑥的语境是用户画像**（对话中采集的预算 / 偏好 / 场景），不是账号本身——那批数据 M2 之后才有。
2. **实现「可删除」从来不靠这一列**。就算留着 `deleted_at`，只要没有「删除账号 / 删除画像」的接口，红线依然没落地；反过来，真删行 + 审计日志留存 userId 才是 PIPL 的正面做法——**软删严格说并不满足「删除」，因为数据还在库里**。
3. 本轮不提供注销接口，属于**已声明的有意偏差**（见 §7.1），不是遗漏。

将来做注销时，正确做法是**真删行 + 审计日志保留 userId**，而不是补一列 `deleted_at`（它属于"当前状态"，需要时 INSTANT 加回）。

### 1.2 `user_sessions`

> ⚠️ **2026-09-24 按项目已拍板口径回写**（原稿是 `BIGINT UNSIGNED` + `DATETIME(3)` + `created_at` 命名）。三处冲突是**技术性的、不是命名洁癖**：

```sql
CREATE TABLE user_sessions (
  id          INT          NOT NULL AUTO_INCREMENT,
  user_id     INT          NOT NULL   COMMENT '关联 users.id，类型必须一致',
  token_hash  CHAR(64)     NOT NULL   COMMENT 'session token 的 sha256(hex)，正好 64 字符',
  create_time DATETIME     NOT NULL   DEFAULT (UTC_TIMESTAMP()),
  expire_time DATETIME     NOT NULL,
  revoke_time DATETIME     NULL,
  ip          VARCHAR(45)  NULL,
  user_agent  VARCHAR(255) NULL,
  PRIMARY KEY (id),
  UNIQUE KEY uq_user_sessions_token_hash (token_hash),
  KEY ix_user_sessions_user_id (user_id),
  KEY ix_user_sessions_expire_time (expire_time)
) ENGINE=InnoDB;
```

**为什么改（每条都有非改不可的理由）**：

| 项 | 原稿 | 改成 | 为什么非改不可 |
|---|---|---|---|
| `id` / `user_id` | `BIGINT UNSIGNED` | **`INT`** | `user_id` 关联的是 `users.id`，而 `users.id` 已拍板是 **`INT`**（实现指引 §13.5）。**两列类型必须一致**——不一致时外键建不了、JOIN 会做隐式转换导致**索引失效**。这不是口味，是功能正确性 |
| 时间精度 | `DATETIME(3)` | **`DATETIME`**（秒级） | 项目已拍板秒级（同上 §13.5）。同秒内多条无法排序的代价，已知悉 |
| 时间列命名 | `created_at` / `expires_at` / `revoked_at` | **`create_time` / `expire_time` / `revoke_time`** | 项目命名口径已定为 `create_time`（`users.create_time`、`users.last_login_time`）。**同一项目里两套时间命名，以后写 SQL 必错** |
| `create_time` 默认值 | `CURRENT_TIMESTAMP(3)` | **`DEFAULT (UTC_TIMESTAMP())`** | ① 带括号**必须**：MySQL 表达式默认值要 `DEFAULT (expr)`，不带括号**建表直接 1064 语法错**（实现指引 §13.6 坑三实测）② 用 `UTC_TIMESTAMP` 而非 `CURRENT_TIMESTAMP`：后者按 **session 时区**求值，裸客户端写入会存成本地时间、与全库 UTC 口径混在一起 |
| 约束名 | `uk_sess_token` / `idx_sess_user` | `uq_…` / `ix_…` | `app/database/base.py` 的 `naming_convention` 会**自动生成**（`uq_%(table_name)s_%(column_0_name)s`）。手写名字反而与自动生成的不一致 → Alembic 每次 autogenerate 都报假 diff |
| `expire_time` 索引 | 无 | **加 `ix_user_sessions_expire_time`** | 清理过期会话是**已知需求**（设计文档 §B.9 雷点 4：表否则无限增长）。**加索引以后是 `INPLACE` 重活，现在加零成本**——同"路由前缀"的判据 |

**不加外键，有意为之**（原稿也没有）：`get_current_user` 里已经处理了「session 指向的 user 不存在」的情况（设计文档 §B.3 `if user is None: raise 40101`），所以 FK 不提供额外正确性；不加则少一条约束、将来删用户不必先清会话。要加随时可加（`fk_user_sessions_user_id_users`）。

要点：**Cookie 里放原始 token，库里只存 sha256**——即便库被拖，会话也不能被直接盗用。这样「登出 / 踢设备」= 置 `revoke_time`。

---

## 2. 接口契约

### 2.0 统一约定

响应体固定信封：
```json
{ "code": 0, "msg": "success", "data": { } }
```
> 字段名以 `app/core/errors.py` 的 `envelope()` / `success()` 为准（2026-09-23 校正）——
> 实现里是 `msg`，原契约写 `message`。
> ✅ **2026-09-24 实测复核**：前端**已接入**（`utils/api.js` 调 `/auth/register` / `/auth/login`），
> 且**已经按 `msg` 实现**（`api.js` 里读的是 `payload?.msg`）。全项目 `frontend-web/src` 仍搜不到 `api/v1` 引用
> → 口径已一致。**注意这句的时态已变：不是"还没接所以便宜"，而是"已经接对了"。**

**路径口径（2026-09-23 大宋拍板：直接按代码回写）**：**不挂 `/api/v1` 前缀**，路由就是 `/auth/*`。
> 原契约设计的是 `/api/v1/auth/*`（版本化惯例）。大宋明确「直接改契约对齐代码」→ 已回写。
> ⚠️ 代价留痕（不是反对，是备查）：将来真要出 v2 时没有版本号可并列，只能靠域名/网关分流，
> 或届时再补前缀——而补前缀要**同时改前端与后端**，属"对外地址"级改动。**现在改便宜，上线后改贵。**

HTTP 状态码按语义返回，`code` 为业务码。错误码表：

| code | HTTP | 含义 |
|------|------|------|
| 0 | 200 | 成功（`success()` 统一 200；原契约写 `200 / 201` 已按代码回写） |
| 40001 | 400 | 参数校验失败（`msg` 指明字段） |
| 40002 | 400 | 人机验证失败（含过期 / 重复提交） |
| 40101 | 401 | 未登录 / 会话失效 |
| 40102 | 401 | 邮箱或密码错误 |
| 40901 | 409 | 邮箱已被注册 |
| 42901 | 429 | 请求过于频繁 |
| 50000 | 500 | 服务器内部错误 |

### 2.1 注册

```
POST /auth/register
```
请求（字段名以 `app/schemas/user_sche.py` 的 `UserRegister` 为准）：
```json
{ "user_email": "a@b.com", "username": "大宋", "password": "S3cret!pass" }
```
校验：`user_email` 合法且 ≤254；`password` 长度 **6–18**，且**必须同时含字母和数字**
（`settings.pw_min_length` / `pw_max_length` 实测值 + 实测四组用例，见 §7.3-B）。

响应 `200`（原契约写 `201` + 用户对象；**2026-09-23 大宋拍板：按代码回写**）：
```json
{ "code": 0, "msg": "success", "data": "注册成功" }
```
> **为什么只回一个字符串就够了**：注册接口要回答的只有「**建号成功了没有**」。
> 用户信息交给下一轮的 `GET /auth/me` 统一提供——鉴权类接口的返回体越薄，越不容易漏字段。
> 原设计返回 `{id,user_email,username,create_time}`；**若下一轮前端确实需要注册后立刻拿到 `id`，
> 再往 `data` 里加，前后端一起改即可。**
> **硬约束不变**：响应体**物理上不含** `password_hash`；`username` 只是展示名、允许重名，
> **永远不能当登录凭据**（§1.1.3）。

错误：`409/40901` 邮箱已注册；`400/40001` 参数非法。

### 2.2 登录

```
POST /auth/login
```
请求（**2026-09-24 改为极验 GT4 口径**，见 §2.5）：
```json
{
  "user_email": "a@b.com",
  "password": "S3cret!pass",
  "lot_number": "4dc3cfc2cdff448cad8d13107198d473",
  "captcha_output": "...",
  "pass_token": "...",
  "gen_time": "1699999999"
}
```
> **为什么是这 4 个字段、为什么扁平不嵌套**：这 4 个是极验前端回调**原样产出的**，扁平放可以让前端 `...captchaResult` 直接展开。
> 嵌套对象反而要额外写「要么全有要么全无」的校验——扁平写法下 Pydantic 缺一个就报 `400/40001`，**天然就是全有**。
> 4 个字段**全部必填**（本轮定：每次登录都要过验证码，理由见设计文档 §A.2）。

流程（**顺序即安全边界，不能换**）：
```
① 校验验证码（调极验二次校验，见 §2.5）
② 查用户（user_email 规范化后查）
③ 校验密码
④ 写 last_login_time
⑤ 建 session（token 入 Cookie / sha256 入库）
⑥ 下发 Set-Cookie
```
> ⚠️ **① 必须在 ② 之前**：验证码不过的请求**根本不碰数据库**。倒过来的话，攻击者不用过验证码就能拿 `/auth/login` 刷你的库。

错误补充：`400/40002` 验证码校验失败（含过期 / 重复提交）；`429/42901` 触发限流（M6 落地，见 §2.5 末尾）。

响应 `200`（原契约返回用户对象；**2026-09-23 大宋拍板：按代码回写**）：
```json
{ "code": 0, "msg": "success", "data": "登录成功" }
```
> 与注册同理：登录只回答「**凭据对不对**」，用户信息由 `GET /auth/me` 统一提供。
> 🔴 **待补（本轮收尾项）**：`last_login_time` 实测登录后仍为 `NULL`。**它不靠 `default` 解决**——
> `default` 只在 `INSERT` 生效、登录是 `UPDATE`；必须在登录成功后**显式赋值**。见 §7.3-C。
响应头：
```
Set-Cookie: session=<token>; HttpOnly; SameSite=Lax; Path=/; Max-Age=604800
              （生产环境追加 Secure）
```
错误：`401/40102`——**统一提示"邮箱或密码错误"，不区分账号是否存在**（防用户枚举）。

### 2.3 当前用户

```
GET /auth/me        （依赖 Cookie）
```
> ✅ **2026-09-24 大宋拍板：路径定为 `/auth/me`**（挂在现有 `auth_router` 下，不单开 users router）。
> 代价留痕（备查，非反对）：将来 `GET /users/{id}`、`PATCH /users/me` 这类 **user 资源接口**要么挤进 `auth_router`（"认证"里读资料），要么那时再拆——而拆就是改对外地址。

响应 `200` —— **这是用户信息的唯一出口**：
```json
{ "code": 0, "msg": "success",
  "data": { "id": 1, "user_email": "a@b.com", "username": "大宋", "create_time": "2026-09-24T03:10:00Z" } }
```
- **为什么要有这个接口**：§2.1 / §2.2 都只回一个字符串，正是为了让用户信息**只有一个出口**——字段加一处，前后端只对一次。
- 🔴 **硬约束**：`data` 里**物理上不含** `password_hash`。靠 `UserResp` 的字段白名单实现，**不靠"记得别返回密码"**（实现指引 §9：一旦写成 `vars(user)`，`password_hash` 就会原样带出去）。
- ⚠️ **时间格式必须带 UTC 标记（`Z`）**：库里是**不带时区的 DATETIME、存的是 UTC**。若序列化成 `"2026-09-24T03:10:00"`（无 `Z`），前端 `new Date(...)` 会按**本地时区**解析 → **显示差 8 小时**（正是本项目已踩过的坑"时间比预期差 8 小时"）。SQLAlchemy 默认不会补 `Z`，需在 schema 层显式处理。
- 错误：`401/40101` 未登录或会话失效。

### 2.4 登出

```
POST /auth/logout
```
流程：按 Cookie 定位 session → 置 `revoke_time` → 清 Cookie（`Set-Cookie` 带 `Max-Age=0`）。
响应 `200`：`{ "code": 0, "msg": "success", "data": null }`
> **幂等设计（本轮定）**：**不带 Cookie / session 已失效，也返回 `200`**，不报 `401`。
> 理由：登出的语义是「保证我现在是未登录状态」。若返回 401，前端会陷入"登出失败 → 要不要重试 → 但用户已经想走了"的怪异状态。
> 副作用是它**不能用来探测会话是否有效**——正合我意。

### 2.5 人机验证（极验 GT4 滑块）

> 🔄 **2026-09-24 整体重写**。原契约是「自研图形码 + `GET /auth/captcha` 返回 `image_base64`」，**已废弃**（大宋拍板改国内厂商滑块）。
> **废弃原因不是强度，是流程方向相反**：自研图形码是「后端出题、后端判卷」，滑块是「**前端出题判卷、后端向厂商复核**」——后端不再生成图片，所以那个接口**整个不成立**。

```
⚠️ 本轮没有后端验证码接口。
```

**流程（三段，跨前后端和厂商）**：

```
① 前端  引入极验 JS → 以 captcha_id 初始化 → 用户滑动 → 回调拿到 4 个参数
                    （lot_number / captcha_output / pass_token / gen_time）
② 前端  把这 4 个参数随 POST /auth/login 一起提交（见 §2.2）
③ 后端  拿这 4 个参数调极验二次校验接口，result == "success" 才放行
```

**③ 的接口形状（实测自官方文档，2026-09-24）**：

| | |
|---|---|
| 地址 | `POST https://gcaptcha4.geetest.com/validate` |
| 内容类型 | `application/x-www-form-urlencoded` |
| 请求参数 | `lot_number` `captcha_output` `pass_token` `gen_time` `captcha_id` `sign_token` |
| `sign_token` | **`HMAC-SHA256(captcha_key, lot_number)` 的 hex** —— 只有一行，防篡改的核心 |
| 响应 | `{"result": "success" \| "fail", "reason": "..."}` |
| 判定 | **`result == "success"` 才通过**；`fail` 时 `reason` 形如 `pass_token expire`、`lot_number重复提交` |

**关键性质**：

- **`captcha_id` 可以公开（放前端），`captcha_key` 是密钥（只能在后端）**——`sign_token` 正是为了让"知道 `lot_number` 但不知道 key 的人"伪造不出合法请求。
- **厂商侧自带防重放**（同一个 `lot_number` 二次提交返回 `fail`）→ **不需要用 Redis 存 `lot_number` 做一次性**。这是相对自研图形码的一个直接简化。
- **失败一律归 `400/40002`**，`msg` 用统一文案「人机验证失败，请重试」。⚠️ **不要把厂商的 `reason` 原样透给用户**（`lot_number重复提交` 这类是实现细节，且可能帮助攻击者调参）。
- **厂商服务异常时怎么办（fail-open / fail-closed）**：**设计阶段定，见设计文档 §A.5**。

**分层防护的对齐**（原设计文档 §A.2 的三层）：
| 层 | 本轮 | 说明 |
|---|---|---|
| 验证码 | ✅ 本轮做 | 极验 GT4 滑块 |
| 限流（IP + email） | ⬜ **归 M6** | `开发流程.md` §2 已把「限流」列为 M6 交付物。**极验本身已挡住脚本**（伪造不了 `sign_token`），限流从"兜底"降级为"防额度消耗" |
| 连续失败锁定 | ⬜ 后续 | 与限流同批设计 |

> 完整设计、雷点与施工顺序见 `docs/登录人机验证与登录态-设计.md` §A。**原 §A.3 / §A.4（Redis 存答案 / GETDEL 一次性 / 大小写归一）整节作废**——那三条都是自研图形码专属。

---

## 3. 安全与合规自查（写代码前先过一遍）

1. 密码哈希：`bcrypt`（cost ≥ 12）或 `argon2id`；**禁** md5 / sha1 / 明文。
2. session token：`secrets.token_urlsafe(32)`（≥160 bit 随机）；库内只存 sha256。
3. Cookie：`HttpOnly` + `SameSite=Lax` + `Path=/`；生产 `Secure=true`。
4. CSRF：`SameSite=Lax` 挡掉大部分跨站 POST；状态变更接口再校验自定义头（如 `X-Requested-With`）或双提交 token。
5. 防枚举：注册/登录失败信息不泄露账号是否已存在。
6. 限流：登录/注册按 `IP + email` 限流（如 5 次 / 分钟）。
7. 日志脱敏：**不落**明文密码、session token；邮箱可脱敏为 `a***@b.com`。
8. PII 最小化：仅 `email + 时间戳`（红线⑥）。**不收集昵称、头像、手机号**——用不到就不收，这是 PIPL 最小必要原则的直接体现。
9. trace_id：中间件生成，响应头回传 `X-Trace-Id`，全链路日志带上（红线⑧）。
10. 命名：全文用「查询 / 采集 / 情报」，**禁用「爬取」字样**。

---

## 4. 目录结构（建议）

> 实际落地：本仓库把 `app/` 放在**仓库根**（与 `frontend-web/` 平级），未套一层 `backend/`。

```
price-agent/                          # 实际落地：app/ 在仓库根，未套 backend/
├── app/
│   ├── main.py                       # FastAPI 入口 + 中间件（trace_id / CORS）
│   ├── core/
│   │   ├── config.py                 # pydantic-settings，读 .env
│   │   ├── security.py               # 哈希 / 校验 / token 生成
│   │   ├── logging.py                # 结构化日志
│   │   └── errors.py                 # 统一错误码 + 异常处理
│   ├── db/
│   │   ├── session.py                # engine / SessionLocal
│   │   └── base.py                   # DeclarativeBase
│   ├── models/                       # user.py, user_session.py
│   ├── schemas/                      # auth.py, user.py（Pydantic 契约）
│   ├── services/auth_service.py      # 业务逻辑（唯一性 / 哈希 / 会话）
│   ├── api/v1/                       # router.py, auth.py, users.py
│   └── deps.py                       # get_db / get_current_user
├── tests/                            # conftest.py, test_auth.py
├── alembic/                          # MySQL 迁移脚本
├── requirements.txt
└── .env.example
```

> 先写 `schemas/`（契约）→ 再写 `models` → 再写 `services` → 最后 `api`。**不要边写边想字段。**
>
> ⚠️ **上面是「建议结构」，实际差异（2026-09-23 实测）**：目录名用 **`database/`**（不是 `db/`）、
> **`router/`**（不是 `api/v1/`）；**`services/` 还没建**——目前密码强度判断、唯一性判断、错误文案
> 都写在 `router/log_in.py` 里。按 §4.1 的「api → services」单向依赖，这些**该下沉到 `services/auth_service.py`**：
> router 只做「接参 → 调 service → 包信封」，不该自己判断业务规则。

### 4.1 依赖方向（铁律）

- 主链单向：`api → services → db/models`，**下层禁止 import 上层**（否则循环依赖必爆）。
- `schemas`（Pydantic 契约）被 `api` 与 `services` 共用；`services` 对外返回 dict / Pydantic，**不把 ORM 对象穿过层边界**（防 session 泄漏）。
- `core`（配置 / 安全 / 日志 / 异常）全层可用，但 `core` 自身不依赖任何业务模块。
- 所有 ORM 模型统一在 `app/models/__init__.py` 汇总 import，保证 Alembic 能发现全部表。

### 4.2 后续里程碑预留目录（现在不建，先留位置）

```
app/
├── agents/      # M1–M2  LangGraph A0~A5 状态图与节点
├── adapters/    # M5     联盟 API 统一适配层（Mock 与真实同签名）
└── rag/         # M3     检索增强：活动规则 / 券知识 / 规格库
```

### 4.3 命名约定

- 目录用复数（`models` / `schemas` / `services` / `api`）；文件用业务域名单数（`user.py`、`auth.py`）。
- ORM 类用单数名词（`User`、`UserSession`）；Pydantic 用后缀区分用途（`RegisterReq`、`UserResp`）。
- 一模块一文件；**禁止**把所有东西塞进 `main.py`。

---

## 5. 依赖（requirements.txt）

```
fastapi
uvicorn[standard]
sqlalchemy>=2.0
aiomysql                # 异步 MySQL 驱动
cryptography            # MySQL 8 caching_sha2_password 认证需要
greenlet                # SQLAlchemy async 运行时依赖
pydantic>=2
pydantic-settings
email-validator         # Pydantic EmailStr
bcrypt
alembic
pytest
pytest-asyncio          # 异步测试
httpx                   # 测试用 ASGI 客户端
```

---

## 6. 自测用例（最低要求，每轮交付前跑）

- [ ] 注册成功 → `201`，响应体**无** `password_hash`
- [ ] 同邮箱重复注册 → `409 / 40901`
- [ ] 非法邮箱 / 弱密码 → `400 / 40001`
- [ ] 登录成功 → `200` 且响应含 `Set-Cookie`
- [ ] 错密码 → `401 / 40102`
- [ ] 带 Cookie 访问 `/me` → `200`；不带 → `401 / 40101`
- [ ] 登出后再访问 `/me` → `401`
- [ ] 并发同邮箱注册（唯一索引兜底）→ 仅一个成功
- [ ] 库里 `users.password_hash` 不是明文

---

## 7. 开工前决策（✅ 已全部拍板 · 2026-09-19）

| # | 决策点 | 结论 | 影响面 |
|---|---|---|---|
| 1 | MySQL 是否就绪 | ✅ 已就绪（本机 MySQL 8.0 运行中，见 §9） | — |
| 2 | 会话有效期 | **7 天 + 滑动续期**（`Max-Age=604800`；剩余不足 3 天才顺延） | 本轮（会话机制） |
| 3 | 密码复杂度 | ✅ **已定（2026-09-23 按代码 + 实测回写）**：**6–18 位，且必须同时含字母和数字**。实测 `abcdefgh` / `12345678` 均被拒、`abc123` 通过、19 位被拒 → 见 §7.3-B | 注册校验 + `core/security.py` |
| 4 | 是否本轮做邮箱激活 | **不做**（需邮件服务，留到后面） | 注册保持两步流程 |
| 5 | 前端联调时机 | **后端 `/me` 跑通后再做** | ⚠️ **实测 2026-09-24：前端登录页已提前落地**（`AuthView.vue` / `utils/api.js` / `vite` 代理 `/auth`），但**是"假登录"**——无 Cookie、无守卫。真实登录态待本轮补 |
| 6 | 实现范围与分轮 | **第 1 轮（已结 2026-09-24）**：只做注册 + 登录，端到端实测通过 → **第 2 轮（本轮，2026-09-24 大宋定）**：**会话（`user_sessions` + `Set-Cookie` + `/auth/me` + 登出 + 滑动续期）与验证码（极验 GT4）同轮做**；限流 / 连续失败锁定 → 归 M6 | 见 §7.1 |
| 7 | 目录分层 | **沿用现有结构 + 最小必要收敛**（不整体重构到 §4 的 `db/ models/ api/v1/`） | 见 `docs/注册登录-实现指引.md` §1 |
| 8 | 是否加 repository 层 | **不加**（§4.1 的分层里本就没有；在 SQLAlchemy 下会退化成纯转发）⚠️ 但代码已建 `app/repository/auth_repo.py` → 见 §7.3-B4 | 见实现指引 §10.1 |
| 9 | `users` 字段取舍 | **6 个**（`id` `user_email` `username` `password_hash` `create_time` `last_login_time`）；删 `nickname` / `status` / `updated_at` / `deleted_at`。9-20 曾定「只 5 个」，9-21 加回 `username` | §1.1.1 / §1.1.2（判据：时间点事件 vs 当前状态） |

### 7.1 本轮与契约的**有意偏差**（实现时知悉）

| 契约原文 | 第 1 轮实际 | 原因 / 现状 |
|---|---|---|
| §2.2 登录请求含验证码字段 | **不含** | 人机验证在第 1 轮未做 → ✅ **本轮补**（字段已按极验 GT4 重写为 4 个，见 §2.2） |
| §2.2 登录成功后 `Set-Cookie: session=...` | **不下发** | 会话机制在第 1 轮未做 → ✅ **本轮补** |
| §2.3 `/me`、§2.4 登出 | **不实现** | 第 1 轮未做 → ✅ **本轮补**（`/me` 路径已定为 **`/auth/me`**） |
| §1.1 原设计含 `nickname` / `status` / `updated_at` / `deleted_at` 四列 | **不建这四列** | 无需求来源（见 §1.1.1）。MySQL 8 加列走 `ALGORITHM=INSTANT`、不重建表，将来真需要再加 |
| §2.2 登录响应含 `nickname` | **不含** | 同上 |

> 这些偏差是**增量的**，不是推翻契约：本轮补上字段与接口后，本契约自动回到一致。

### 7.2 补充：本轮新增的安全口径（原 §3 未覆盖）

| 项 | 口径 | 为什么 |
|---|---|---|
| **时间时区** | 全库统一 **UTC**（连接层 `SET time_zone='+00:00'`；Python 写朴素 UTC） | MySQL `DATETIME` 不带时区。若 `NOW()`（服务器本地）与 Python 写入混用会**差 8 小时**——实测：本地 17:06 时 `NOW()` 返回 09:06。ACTP 上已踩过此坑 |
| **密码字节上限** | 截断 **72 字节**（哈希与比对必须用**同一规则**） | bcrypt 只取前 72 字节，且 4.x 对超长输入**直接抛 `ValueError`** |
| **哈希成本** | bcrypt `cost=12` | 实测 ~185ms/次；爆破 100 万密码约 **51 小时**（同量级 sha256 只需 **0.7 秒**） |
| **参数校验失败返回码** | 必须**接管** `RequestValidationError` → `400 / 40001` | 否则 FastAPI 默认返回 **422** + 自带格式，与 §2.0 信封不符 |
| **响应体约束** | 出参模型**物理上不含** `password_hash` | 不靠"记得别返回"，靠**类型隔离** |
| **审计** | 每次请求带 `trace_id`，响应头回传 `X-Trace-Id` | 红线⑧ |

---

### 7.3 契约 vs 代码：何时跟代码走，何时必须先问（2026-09-23 定）

> **大宋的口径**：别死抠契约。发现不一致时，先给「**不按契约的影响**」；
> **影响不大 → 直接按代码回写本文**；**影响大 → 摆清楚影响、等他一句话**，他没回应再回写。
> 判断「影响大不大」只看两条，命中任意一条就必须先问：

| # | 判据 | 为什么 |
|---|---|---|
| **① 撞已拍板的决策** | 改它等于改他**自己拍过板**的决定（§7 表格各项、§1.1 字段取舍） | 契约是**小宋维护的文档，可以改**；拍板是**他的决定，不能替他改** |
| **② 撞安全红线 / 对外地址** | 用户枚举文案、密码强度、哈希口径、`Set-Cookie`、**路由前缀**（现口径 `/auth/*`，见 §2.0） | 改错的代价不是"文档难看"，是**泄露**或**前后端对不上**；一旦发布就改不动 |

#### A. 已直接按代码回写（影响不大，本轮已落）

| 项 | 原契约 | 已改成（跟代码） | 不跟的影响 |
|---|---|---|---|
| 信封字段 | `message` | **`msg`**（`core/errors.py` 实证） | 前端读 `message` 得 `undefined`。**2026-09-24 实测复核：前端已接入且已按 `msg` 实现**（`utils/api.js` 读 `payload?.msg`），全项目搜不到 `api/v1` 引用 → 口径已一致 |
| 成功文案 | `"ok"` | **`"success"`** | 同上前提 |
| 请求字段 | `email` | **`user_email`**（`schemas/user_sche.py`） | 同上 |
| 注册请求 | 无 `username` | **加 `username`** | 同上 |
| 时间字段 | `created_at` / `last_login_at` | **`create_time`** / **`last_login_time`** | 同上（§1.1 命名口径已定） |
| 密码长度 | 8–64 | **6–18**（`settings` 实测值） | 长度松紧是策略选择，无结构性后果；18 上限有依据（bcrypt 72 字节） |
| 邮箱列长 | `VARCHAR(100)` | **`VARCHAR(255)`**（`models/user.py`） | 更宽松，撞不到 RFC 的 254 上限 → **注册不会因超长邮箱报 500**。§1.1 / §1.1.2 已同步 |
| Pydantic 模型名 | `RegisterReq` / `LoginReq` | **`UserRegister`** / **`UserLogin`** | 纯内部命名 |
| 目录结构 | `backend/app/db/`、`api/v1/` | **根 `app/database/`、`router/`** | 已在 §4 注明 |

#### B. 必须先问、等大宋一句话

| # | 项 | 契约 | 实现 | 状态（2026-09-23 19:50 实测校准） |
|---|---|---|---|---|
| 1 | **密码强度逻辑** | 含字母 **且** 数字 | `not _has_letter or not _has_digit`（= **且**） | ✅ **已解决**：实测 `abcdefgh`（纯字母）、`12345678`（纯数字）**均被拒**，`abc123` 通过 → 代码已符合契约，**只剩文案**（见 C 表） |
| 2 | 密码长度下限 | ≥8（§7 #3） | `config` 实测 **6**（区间 6–18） | ⚠️ **已按代码回写**：实测 5 位被拒、`abc123`(6 位) 通过、18 位通过、19/20 位被拒 → §2.1 与 §7 #3 已改为 **6–18**。要恢复 8 说一声，改 `config` 一行 |
| 3 | `users` 字段 | 9-20 拍「只 5 个」，9-21 加回 `username` | 6 个 | ✅ 已回写 §1.1（确认 `username` 是有意加的） |
| 4 | **repository 层** | 9-20 拍「**不加**」 | 已建 `app/repository/auth_repo.py` 且**在用** | 🔴 **撞已拍板决策，仍需你一句话**：有意改主意 → 我更新 §7 #8 与实现指引 §10.1；顺手建的 → 合并回 service。**不按契约的实际影响**：`user_create` 自己 `commit`，事务边界碎在 repo；将来"注册 + 发欢迎券 + 写审计"要原子提交时会做不了，而那时调用点已变多 |
| 5 | **验证码方案** | §2.5 `GET /auth/captcha` 返回 `image_base64`（自研图形码）；Redis 存答案、GETDEL 一次性 | **2026-09-24 大宋定：改极验 GT4 滑块** | ✅ **已按拍板回写**（原为 🔴 待拍板项，大宋选定厂商后即为「已拍板的对外地址变更」，故直接落）<br>落点：① §2.5 **整节重写**为极验版（`GET /auth/captcha` **已删**，改为说明"本轮无后端验证码接口"）② §2.2 登录字段 → 4 个极验字段 ③ 设计文档 §A.3/§A.4（Redis 存答案 / GETDEL / 大小写）**作废** ④ `requirements.txt` 加 `httpx`、`Pillow` 可删 ⑤ `.env` 加厂商密钥<br>**核心形态变化**：滑块是「**前端出题判卷、后端向厂商复核**」，与自研图形码方向相反 |
| 6 | **厂商服务异常时的策略**（fail-open / fail-closed） | 契约未覆盖（自研图形码无外部依赖，不存在此问题） | 未定 | 🟡 **需大宋拍板**：极验不可用时，登录是**放行**（可用性优先）还是**拦截**（安全优先）？见设计文档 §A.5——小宋建议**开发期 fail-open + 打 WARNING**，M6 加固时翻 fail-closed |

#### C. 收尾清单（状态见右列；「改代码」项需大宋动手）

| 项 | 实测（2026-09-24 11:10 复验） | 状态 |
|---|---|---|
| `main.py` 缺 `include_router` | 路由表实测已可见 `POST /auth/login`、`POST /auth/register` | ✅ **已解决** |
| 4 处 `raise ValueError` | 业务层已换 `BizError`；实测弱密码 → `400/40001` | ✅ **已解决** |
| 路由前缀 | 实测 `POST /auth/login` | ✅ **已按代码回写契约**（2026-09-23 大宋拍板）：§2.0 路径口径 + §2.1~§2.5 去掉 `/api/v1`，统一 `/auth/*` |
| 响应体不是用户对象 | 实测 `data:"注册成功"` / `"登录成功"`（字符串） | ✅ **已按代码回写契约**（2026-09-23 大宋拍板）：§2.1/§2.2 响应体改为字符串；用户信息由下一轮 `/me` 统一提供 |
| 登录失败文案 | 实测：错密码 → `40102 "邮箱或密码错误"`；**不存在的邮箱 → 同一文案** | ✅ **已解决**：文案已在 `auth_repo.get_pw_hash` **一处收口**（2026-09-24），枚举面堵住 |
| `last_login_time` 不更新 | 实测：正确密码登录后写入 `2026-09-24 03:10:00`（北京 11:10 → **正好 UTC**）；错密码后仍为 `NULL` | ✅ **已解决**：新增 `auth_repo.user_last_login_time()`，`func.utc_timestamp()` 显式写；调用位置在密码校验**之后**（顺序实测正确） |
| 强度文案与规则矛盾 | **2026-09-24 11:24 实测：`security.py:28` 已是「密码必须包含字母**且**数字」，与 `not letter or not digit → raise` 同向** | ✅ **已解决**（文案措辞可再顺为"必须同时包含字母和数字"，不阻塞） |
| 无中间件 / 无 `/health` | 实测 `app.user_middleware` = **0**，路由表无 `/health` | 🟡 **未做**。属 `开发流程.md` M0 退出条件 + §7.2 审计口径（响应头 `X-Trace-Id`） |


## 8. 数据库双库分工（MySQL + Milvus）

> 决策：业务库 = **MySQL 8**，向量库 = **Milvus**（替代设计稿 §5 的 `PostgreSQL + pgvector`）。

| 库 | 职责 | 存什么 | 里程碑 |
|----|------|--------|--------|
| **MySQL 8** | 业务 / 结构化数据（唯一事实源） | 用户、会话、对话、画像、推荐记录、审计日志 | 本轮起 |
| **Milvus** | 向量检索（RAG） | 活动规则 / 券知识 / 商品规格库的 embedding；**只存向量 + doc_id** | M3 |

关键约定：
1. **原文与向量分离**：知识原文（规则文本、券说明、规格）存 MySQL；Milvus 只存 `embedding + doc_id + 少量标量过滤字段`。检索命中后按 `doc_id` 回 MySQL 取原文。
2. **一致性**：知识更新时先写 MySQL、再 upsert Milvus（跨库，需补偿 / 重试，不做分布式事务）。
3. **embedding 维度**：建 collection 时锁定维度（与所选 embedding 模型一致，如 1024）；换模型必须重建 collection。
4. **部署**：开发用 Milvus Standalone（Docker Compose）；生产可上集群版。Windows 下 Milvus Lite 支持有限，优先 Docker。
5. **本轮无关**：用户注册 / 登录只碰 MySQL，不需要先装 Milvus；Milvus 到 M3 再接。

## 9. 开工顺序与本机环境

### 9.1 本机环境实测（2026-09-13）

- **MySQL 8.0 已安装且在运行**：`127.0.0.1:3306`（另 `33060` = X Protocol）；`root` 需密码（免密连接被拒 → 认证配置正常）。
- 命令行客户端：`C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe`（未加入 PATH）。
- 已装：MySQL Server / Workbench / Shell / Router 8.0；**未装 Docker**（M3 上 Milvus 时再装）。
- Python 3.13 可用。

### 9.2 推荐开工顺序（先基础设施、后业务）

> 原则：**基础设施先于业务**。数据库连接属基础设施（`core` + `db`），与业务契约正交；但业务侧仍必须 **契约 `schemas` 先于 `services`**。

1. **建库 + 建账号**（一次性 SQL，见 9.3）
2. `requirements.txt` + `.env` / `.env.example`（连接参数不进 git）
3. `app/core/config.py` —— pydantic-settings 读取 `DATABASE_URL`
4. `app/db/base.py`（DeclarativeBase）+ `app/db/session.py`（engine / SessionLocal / get_db）
5. **验证连接**：加一个 `GET /health`（内部 `SELECT 1`）或最小脚本，**确认连通后再往下写**
6. `app/models/` → Alembic 迁移建表 → `schemas` → `services` → `api`

### 9.3 建库建账号（MySQL，一次性执行）

```sql
CREATE DATABASE IF NOT EXISTS price_agent
  DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE USER IF NOT EXISTS 'price_agent'@'localhost' IDENTIFIED BY '<强密码>';
GRANT ALL PRIVILEGES ON price_agent.* TO 'price_agent'@'localhost';
FLUSH PRIVILEGES;
```
> 应用**不要用 root** 连库；开发也用专用账号，最小权限。

### 9.4 连接串与 MySQL 专属坑

- URL 格式（异步）：`mysql+aiomysql://price_agent:<密码>@127.0.0.1:3306/price_agent?charset=utf8mb4`
- 异步栈额外依赖：`aiomysql` + `greenlet`。**异步 session 获取规范见 `docs/代码审查-异步连接与Session规范.md`**。
- 依赖必须含 **`cryptography`**（MySQL 8 默认 `caching_sha2_password` 认证需要）。
- **连接池**：`create_engine(..., pool_pre_ping=True, pool_recycle=3600)` —— 否则空闲久了报 `MySQL server has gone away`。
- **时区**：MySQL `DATETIME` 不带时区。建议统一按 UTC 存，或连接时 `init_command="SET time_zone='+08:00'"`。**ACTP 上踩过"时间字段慢 8 小时"的坑，这里必须一开始就定死时区口径。**
- `echo=True` 只在开发开，生产关（避免刷屏 + 泄漏 SQL 参数）。
- 建表：开发初期可用 `Base.metadata.create_all()` 快速验证，**但正式表结构一律走 Alembic**，禁止线上 `create_all`。

---

*本文件为「设计先行」契约定稿草案；评审通过后据此编码。
⚠️ **代码与本文不一致时的处理口径见 §7.3**：默认**以代码为准回写本文**；
撞「已拍板决策」或「安全红线 / 对外地址」时，先摆影响、由大宋拍板。*
