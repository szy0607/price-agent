# 登录人机验证与登录态 — 设计

> 日期：2026-09-19 ｜ **v2 重写：2026-09-24** ｜ 归属：`开发流程.md` §3 第②步「设计先行」
> 定位：`docs/用户注册与登录-接口契约.md` 的**第 2 轮补充**（契约说「做什么」，本文说「为什么这么设计、按什么顺序动手」）
>
> **v2 变更（2026-09-24）**：
> - **§A 整节重写**：自研图形码（Pillow + Redis）→ **极验 GT4 滑块**。原因见 §A.1（**流程方向相反**，不是强度问题）
> - **§A.5 新增待拍板项**：厂商故障时 fail-open / fail-closed
> - **§B 口径校正**：列名跟契约 §1.2 走（`expire_time` / `revoke_time`）；异常用项目自己的 `BizError`（原伪代码写的 `AuthError` **不存在**）
> - **§D 重写为「第 2 轮施工顺序」**，旧清单移入附录存档
> - **本轮不需要 Redis**（限流归 M6，且极验自带防重放）——比原设计少一整套机制

> ⚠️ 本文**不含业务代码**，给出的是形状 / 顺序 / 理由。**代码由大宋实现。**

---

## A. 人机验证（验证码）

> 🔄 **2026-09-24 整体重写（极验 GT4 版）**。原版是「自研图形字符码（Pillow）+ Redis 存答案」，**已废弃**。
> 废弃原因：大宋 2026-09-24 拍板改用**国内厂商滑块**。两者**流程方向相反**——见 A.1。

### A.1 方案选择：为什么换厂商，为什么是极验

| 方案 | 抗破解 | 后端依赖 | 结论 |
|---|---|---|---|
| 自研图形字符码（Pillow） | 低 | Pillow + Redis | ❌ 放弃 |
| **极验 GT4** | 高 | `httpx`（纯异步，无 SDK） | ✅ **本轮采用** |
| 腾讯云验证码 | 高 | 官方 Python SDK **是同步的** | ❌ 在异步项目里会阻塞事件循环 |

**为什么放弃自研图形码（不只是"强度低"）**：

1. **强度确实低**——你们在 ACTP 用 cnocr 破过若依的图形码，这条经验有效。
2. **但真正的转折点是流程方向**：自研图形码是「**后端出题、后端判卷**」；滑块是「**前端出题判卷、后端向厂商复核**」。
   方向一反，后端就不再生成图片 → 契约 §2.5 那个 `GET /auth/captcha` 接口**整个不成立**。
   → **换方案不是换一个库，是换一套流程。**

**为什么在腾讯和极验之间选极验**（下表实测自两家官方文档，2026-09-24）：

| | 腾讯云验证码 | **极验 GT4** |
|---|---|---|
| 后端调用 | `DescribeCaptchaResult` @ `captcha.tencentcloudapi.com` | `POST gcaptcha4.geetest.com/validate`（form 表单） |
| 前置条件 | **要腾讯云 API 密钥**（SecretId/Key） | 只要 `captcha_id` + `captcha_key` |
| 签名 | TC3-HMAC-SHA256（官方 SDK 代劳） | **一行** `HMAC-SHA256(captcha_key, lot_number)` |
| Python SDK | **官方 SDK 是同步的** → 在 `async def` 里调会**阻塞事件循环** | 无 SDK，`httpx.AsyncClient` 直接 POST，**纯异步** |
| 本地开发 | 要开通服务 + 配域名白名单 | **官方有公开测试账号**，立刻可联调 |
| 失败返回 | `CaptchaCode`：1 通过 / 7 randstr 不匹配 / 8 过期 / 9 复用 / 15 解密失败 / 16 appid 不匹配 | `result: success/fail` + `reason` |

> 📌 **第 4 条是决定性的**。腾讯那条路要额外写 `await asyncio.to_thread(...)` 才能不阻塞，
> 正好撞本项目《异步连接与Session规范》的红线。**能不引入同步 IO 就不引入。**

### A.2 分层防护：三层里本轮做哪层

| 层 | 做什么 | 本轮 | 为什么 |
|---|---|---|---|
| 1 | **限流**（IP + email 计数） | ⬜ **归 M6** | `开发流程.md` §2 已把「限流」列为 M6 交付物；**且极验本身已挡住脚本**（`sign_token` 伪造不了）→ 限流从"兜底"降级为"防额度消耗" |
| 2 | **验证码** | ✅ **本轮** | 极验 GT4 |
| 3 | **连续失败锁定** | ⬜ 后续 | 需 Redis 计数，与限流同批设计 |

> ⚠️ **因此本轮不需要 Redis**。这是相对原设计的一个直接简化：
> 原设计用 Redis 存验证码答案（`captcha:{id} → code`，GETDEL 一次性）；
> **极验的 `lot_number` 厂商侧自带防重放**（重复提交返回 `fail`）→ 后端不用自己存状态。
> → **`fast` 环境本轮也不用装 `redis` 包。**

**每次登录都要验证码，还是失败 N 次后才要？本轮定：每次都过。** 理由：

- 「失败 N 次后才要」需要前端先问后端"这次要不要验证码" → **多一个探测接口**，
  而这个接口本身会泄露"该邮箱是否已被锁定"——**为一个体验优化开一个新的信息泄露面，不划算。**
- 前端还要维护"上次失败了没有"的状态，前后端状态同步是额外的 bug 源。
- 登录不是高频操作，每次滑一次的成本可接受。

### A.3 流程与接口形状

**三段流程（跨前后端和厂商）**：
```
① 前端  引极验 JS → captcha_id 初始化 → 用户滑动 → 回调拿到 4 参数
                 （lot_number / captcha_output / pass_token / gen_time）
② 前端  4 参数随 POST /auth/login 提交
③ 后端  调极验二次校验 → result == "success" 才放行
```
**接口字段以 `docs/用户注册与登录-接口契约.md` §2.2 / §2.5 为准**（那里是唯一事实源，本节只讲形状与理由）。

**③ 的服务端形状**：

| | |
|---|---|
| 地址 | `POST https://gcaptcha4.geetest.com/validate` |
| 内容类型 | `application/x-www-form-urlencoded` |
| 请求参数 | `lot_number` `captcha_output` `pass_token` `gen_time` `captcha_id` `sign_token` |
| `sign_token` | `HMAC-SHA256(captcha_key, lot_number)` → **hex** |
| 响应 | `{"result": "success" / "fail", "reason": "..."}` |
| 判定 | **`result == "success"` 才通过**；`fail` 的 `reason` 形如 `pass_token expire`、`lot_number重复提交` |

> 📌 `captcha_id` **可以公开**（本来就要放前端 JS 里）；`captcha_key` **是密钥**（只能在后端）。
> `sign_token` 的设计意图正是：**知道 `lot_number` 但不知道 key 的人，伪造不出合法请求。**

### A.4 安全与实现要点

**密钥管理（最要紧的一条）**：
- `CAPTCHA_ID` 放前端（`VITE_CAPTCHA_ID`），`CAPTCHA_KEY` **只在后端用**。
- `CAPTCHA_KEY` 要进 `.env` → ⚠️ **`.env` 已经犯过一次入库的错（含 MySQL root 密码）**。
  **加这个 key 之前，必须先确认 `.gitignore` 真的挡住了 `.env`**，否则等于把密钥推到 GitHub。
- **key 绝不进日志**：日志里只记 `lot_number` 和 `result`，不记关键参数。

**失败文案收口**：
- 后端一律返回 `400 / 40002`，`msg` 统一为「人机验证失败，请重试」。
- ⚠️ **不要把极验的 `reason` 原样透给用户**——`pass_token expire` / `lot_number重复提交` 是实现细节，
  透出去既是信息泄露，也可能帮攻击者调参。

**顺序（安全边界，不能换）**：验证码校验**必须在查库之前**。
倒过来的话，攻击者不用过验证码就能拿 `/auth/login` 刷你的数据库。

**`gen_time` 原样回传，不要自己解析**：它不是给你用的，是给厂商判断时间漂移的。
类型用 `str`（不用 `int`）——避免 Pydantic 吃掉前导零或异常格式。

**HTTP 客户端**：用 `httpx.AsyncClient`（项目已有 `httpx`，但只在 `requirements-dev.txt` 里 → **本轮要提到运行依赖**）。
⚠️ 别忘了**超时**：厂商接口卡住时不能把请求无限挂住（建议 3–5s）。

### A.5 🟡 待拍板：厂商服务异常时怎么办（fail-open / fail-closed）

| 策略 | 厂商挂了会怎样 | 好处 | 代价 |
|---|---|---|---|
| **fail-open**（放行） | 用户照常登录 | 登录不被第三方拖死 | 厂商挂的那段时间，**验证码形同不存在**（但密码校验仍在） |
| **fail-closed**（拦截） | **全站登不进来** | 安全性不受第三方影响 | 第三方抖动 = 你的登录不可用 |

**小宋建议：开发期 fail-open + 打 WARNING 日志；M6 加固时翻 fail-closed。**
理由：现在没有真实用户，厂商抖动会直接阻塞你的联调；且此时攻击者的"收益"只是能试密码——
而 `cost=12` 的 bcrypt 已把爆破压到 51 小时/百万次。

**但这条属安全口径，由大宋拍板**（已登记在契约 §7.3-B6）。

> 📌 **无论选哪个，都必须能区分「厂商说 fail」和「厂商连不上」**：
> 前者是用户没过验证码（`40002`），后者是系统故障（`50000` 或按策略放行）。
> **混在一起会让排障时误判成"用户手滑"。**

---

## B. 登录态维持（Session Cookie）

### B.1 生命周期（四环节）

见对话中的流程图：

1. **登录成功 → 发会话**：生成随机 `token`（给浏览器），库里只存 `sha256(token)`（给数据库），`Set-Cookie`。
2. **后续请求 → 校验**：浏览器自动带 Cookie → 依赖查 `user_sessions` 有效则放行，否则 `401/40101`。
3. **临近过期 → 滑动续期**：延长 `expire_time` 并重发 Cookie（节流：剩余不足 3 天才续）。
4. **登出 / 踢设备 → 失效**：置 `revoke_time` + 清 Cookie；改密 / 全端登出 = 按 `user_id` 批量失效。

### B.2 登录成功：发会话

```python
import hashlib
import secrets
from datetime import datetime, timedelta, timezone

token = secrets.token_urlsafe(32)                        # 给浏览器
token_hash = hashlib.sha256(token.encode()).hexdigest()  # 给数据库
expire_time = datetime.now(timezone.utc) + timedelta(days=7)

session.add(UserSession(
    user_id=user.id,
    token_hash=token_hash,
    expire_time=expire_time,
    ip=request.client.host,
    user_agent=request.headers.get("user-agent", "")[:255],
))
await session.commit()

response.set_cookie(
    key="session",
    value=token,
    httponly=True,
    samesite="lax",
    secure=settings.cookie_secure,      # 生产 True
    max_age=7 * 24 * 3600,
    path="/",
)
```

> **会话固定防护**：登录成功后**新发一个会话**，绝不复用登录前的任何标识 —— 标准做法。

### B.3 每次请求：校验依赖

```python
from fastapi import Cookie, Depends


async def get_current_user(
    session: SessionDep,
    session_token: str | None = Cookie(default=None, alias="session"),
) -> User:
    if not session_token:
        raise BizError(ErrorCode.SESSION_INVALID, "未登录")

    token_hash = hashlib.sha256(session_token.encode()).hexdigest()
    row = await session.scalar(
        select(UserSession).where(
            UserSession.token_hash == token_hash,
            UserSession.revoke_time.is_(None),
            UserSession.expire_time > datetime.now(timezone.utc),
        )
    )
    if row is None:
        raise BizError(ErrorCode.SESSION_INVALID, "会话失效，请重新登录")

    user = await session.get(User, row.user_id)
    if user is None:
        raise BizError(ErrorCode.SESSION_INVALID, "账号不可用")
    return user
```

> ⚠️ **2026-09-24 口径校正（三处）**：
> ① 列名跟契约 §1.2 走：`expire_time` / `revoke_time`（原稿写 `expires_at` / `revoked_at`）。
> ② 异常用项目自己的 **`BizError(ErrorCode.…)`**（`app/core/errors.py`），**没有 `AuthError` 这个类**——原稿的 `AuthError` 是伪代码占位。
> ③ `datetime.now(timezone.utc)` 得到的是 **aware datetime**，而库里是**朴素 UTC**（`DATETIME`）→ 比较时**要去掉 tzinfo**（`datetime.now(timezone.utc).replace(tzinfo=None)`），否则 SQLAlchemy 会带时区去比，行为依赖驱动。**这一条务必自测**。

> ⚠️ **2026-09-20 修订**：原伪代码是 `if user is None or user.status != 1 or user.deleted_at is not None`。
> `status` 与 `deleted_at` 两字段**已从 `users` 表删除**（契约 §1.1.1）——照原样写会 `AttributeError`。
> 将来若真做「封号」或「注销」，把这两列加回（INSTANT，不重建表），再把这个条件补回来。

### B.4 持续 / 续期：滑动过期（sliding expiration）

- **纯固定 7 天**：简单，但第 7 天必然被踢，体验差。
- **滑动续期（推荐）**：校验通过后，若 `expire_time - now < 3 天`，则把 `expire_time` 顺延为 `now + 7 天`，并**重发 Cookie**（刷新 `Max-Age`）。
- **节流**：只在"剩余不足阈值"时才写库 + 重发，避免每个请求都 `UPDATE`。
- 另一条路是"短会话（如 2h）+ refresh token"（双令牌），本轮不做。

### B.5 登出 / 踢设备

- **登出**：`revoke_time = now` + `response.delete_cookie("session")`。
- **全端登出 / 改密**：`UPDATE user_sessions SET revoke_time = now WHERE user_id = ?`（**改密必须做**）。
- **清理**：定期删 `expire_time < now` 的行（定时任务，或手动 SQL）。

### B.6 "记住我"

- 勾选 → `max_age = 7 天`（持久 Cookie）。
- 不勾 → **不设 `max_age`**（会话 Cookie，关浏览器即失效）。

### B.7 安全要点（对照契约 §3）

1. Cookie **只放原始 token**，库里只存 **sha256**（泄库也无法直接盗用会话）。
2. `HttpOnly`（防 XSS 读 Cookie）+ `SameSite=Lax`（挡大部分 CSRF）+ 生产 `Secure`。
3. **CSRF**：`SameSite=Lax` 之外，状态变更接口建议再校验自定义头（如 `X-Requested-With`）或双提交 token。
4. **会话固定**：登录后新发会话。
5. **时间口径统一 UTC** —— 若混用 `datetime.now()`（本地）与 `utcnow()`，跨时区会算错（ACTP 有过时间偏差的教训）。
6. 索引：`token_hash` 唯一、`user_id` 普通索引。

### B.8 前端联调（关键）

- 浏览器会**自动带 Cookie**，前端不用手动管 token —— 这是 Session Cookie 相对 JWT 最省事的地方。
- 但 fetch / axios 必须 **`credentials: 'include'` / `withCredentials: true`**。
  ✅ **实测 2026-09-24：`frontend-web/src/utils/api.js` 已经带上了**（`credentials: 'include'`）——前端这块已就绪。
- **开发期用 Vite 代理**：把 `/auth` 代理到 `http://127.0.0.1:8000`，前后端同源 → Cookie 无跨域烦恼。
  ✅ **实测已配**（`frontend-web/vite.config.js`）：
  ```js
  server: {
    proxy: { '/auth': { target: 'http://127.0.0.1:8000', changeOrigin: true } },
  }
  ```
  > ⚠️ 注意代理的是 **`/auth`** 而不是 `/api`——因为后端路由前缀就是 `/auth/*`（契约 §2.0）。
  > **`/auth/me` 也在同一个前缀下，所以代理不用改。** 这正是当初把 `/me` 挂在 `auth_router` 的一个附带好处。
- 若坚持跨域：后端 CORS 必须 `allow_credentials=True`，且 `allow_origins` 写**具体源**（**不能用 `*`**），前端带 `credentials`。
  > ⚠️ 补充：一旦用了 Cookie 会话，**`allow_origins=["*"]` 与 `allow_credentials=True` 不能共存**（浏览器会直接拒绝），
  > 且 `SameSite=Lax` 在跨站请求里**不会发送** Cookie → 跨域部署要改成 `SameSite=None; Secure`（**必须 HTTPS**）。
  > **结论：开发期就走 Vite 代理同源，别碰跨域。**

### B.9 雷点

1. **别把会话存进程内存**（多 worker 失效）—— 用 MySQL（已定）。
2. `set_cookie` 必须拿到 `Response` 对象：用 `response: Response` 参数或返回 `JSONResponse`，否则设不上。
3. `SameSite=None` 必须配 `Secure=True`，否则浏览器直接丢弃 Cookie。
4. 会话表要能清理，否则无限增长。
5. `Cookie(alias="session")` 的 `alias` 要与 `set_cookie(key=...)` 完全一致，写错就永远取不到。

---

## C. 第 1 轮代码的连带问题（复评 · 存档）

> ⚠️ **本节已定格在第 1 轮（2026-09-20 状态）**，**不再是问题跟踪的唯一入口**。
> **当前问题/收尾清单的权威口径在 `docs/用户注册与登录-接口契约.md` §7.3**（A 已回写 / B 待拍板 / C 收尾清单）——
> 查问题请去那里。本节保留作为"这一轮踩过哪些坑"的历史存档。
>
> 📌 **2026-09-24 实测补充**：本节下表里 `main.py` 那条"已修 include_router"是对的，
> 但 **`main.py` 的中间件与 `/health` 至今仍未做**（实测 `app.user_middleware` = 0）→ 属 M0 退出条件欠账，见契约 §7.3-C。

| 级别 | 位置 | 问题 | 状态（09-20） |
|------|------|------|------|
| 🔴 P0 | `app/dependence.py` | 导入路径错 + 拿 sessionmaker 实例做类型注解 + 缺 `rollback` | ✅ **已修**：`get_db_session()` 三处全对，实测真连库通过 |
| 🔴 P0 | `app/database/session.py` | `connect_args` 挂到了 `async_sessionmaker` 上 → 取 session 时 `TypeError`，**同时时区设置静默失效** | ✅ **已修**：挪到 `create_async_engine`；实测 `@@session.time_zone = +00:00` |
| 🟡 P1 | `app/router/register.py`、`app/router/log_in.py` | 前缀 `/register`、`/login` 与契约不符 | ✅ **已解决**（2026-09-23）：`register.py` 已删并合并进 `log_in.py`；前缀口径定为 **`/auth`**（按代码回写契约，不挂 `/api/v1`） |
| 🟡 P1 | `app/main.py` | 还没 `include_router` | ✅ **已修**：实测路由表可见 `POST /auth/login`、`POST /auth/register` |
| 🟡 P1 | `app/model/user.py` | 仍是旧定义：命名不符、`id` 类型待定 | ✅ **已修**：现在 `app/models/user.py`，**6 字段**（9-21 加回 `username`）；实测 DDL `id int AUTO_INCREMENT`、`UNIQUE KEY uq_users_user_email` |
| ⚠️ 更正 | `app/schemas/user_sche.py` | 本节原文写「`User_out` **含 `user_password` 字段**」 | ❌ **该描述有误，特此更正**：核对实际代码，`User_out` 从来没有 `user_password` 字段（原本只有 `user_email`）。**但风险并没消失、只是换了形态**——该文件现已被清空成只剩两行 import，`RegisterReq`/`LoginReq`/`UserResp` 均待新建；真实风险变成「**service 若直接返回 ORM 对象，`password_hash` 会随之回显**」 |
| 🟢 | — | 已修：`echo=False`、`from app.database.base import Base` 断链 | ✅ |

---

## D. 第 2 轮施工顺序（登录态 + 验证码 · 2026-09-24 定）

> 📌 **第 1 轮（注册 + 登录）已结**：端到端实测通过，收尾项已清（实测见契约 §7.3-C）。
> ⚠️ 本文件旧版 §D 第 9 步「`GET /auth/captcha` + Redis 限流」**已因换极验而整体作废**（存档见文末）。

### D.0 本轮范围边界

| | 内容 |
|---|---|
| ✅ 做 | 会话（`user_sessions` 表 → `Set-Cookie` → `get_current_user` → `/auth/me` → 登出 → 滑动续期）+ 验证码（极验 GT4）+ 前端登录态收口 |
| ⬜ 不做 | 限流 / 连续失败锁定（**归 M6**，见 §A.2）；改密 / 全端登出；「记住我」（§B.6）；邮箱激活 |

### D.1 施工顺序（顺序即依赖方向，不能换）

```
① 依赖与配置    requirements.txt 加 httpx（运行依赖）；.env 加 CAPTCHA_ID / CAPTCHA_KEY；
               config.py 加对应字段 → ⚠️ 先确认 .gitignore 挡住 .env
② 模型与迁移    app/models/user_session.py（按契约 §1.2）→ models/__init__.py 补 import
               → alembic revision --autogenerate → 人工逐行审 → upgrade head
③ 安全工具      core/security.py 加 token 生成/哈希（secrets.token_urlsafe(32) + sha256 hex）
               验证码校验独立成 core/captcha.py（极验客户端，唯一出处）
④ 依赖注入      dependence.py 加 get_current_user（**公共依赖，放这里**，不放 router）
⑤ 接口          log_in.py：login 加验证码校验 + 建 session + Set-Cookie；新增 /auth/me、/auth/logout
⑥ 前端          路由守卫 + 用 /auth/me 恢复登录态，替换 AuthView.vue 里的 localStorage 占位
⑦ 自测+真实验证  pytest（契约 §6 清单）+ curl 真起服务验 Cookie 往返
```

**每一步「为什么必须在这个位置」**：

| 步 | 为什么不能提前 / 推后 |
|---|---|
| ① | `.env` 与 `config.py` 是**所有后续步骤的输入**。且"密钥不能入库"这条**必须最先确认**——一旦 commit 出去就收不回来 |
| ② | **迁移先于代码**：模型不落地，`login` 里就没法 `session.add(UserSession(...))`。且迁移要**人工逐行审**——`--autogenerate` 会漏东西（`users` 那轮就撞过 `server_default` 假 diff） |
| ③ | 验证码校验是**纯客户端/纯函数**，不依赖 session、不依赖路由 → 可以先写完**单独测通**（拿官方测试账号真跑一次），再拿到接口里用。**这是本轮唯一的外部依赖，最该先"证明能跑"** |
| ④ | `get_current_user` **必须住 `dependence.py`**：`/auth/me` 要它、`/auth/logout` 要它、将来别的 router 也要它。放 `router/log_in.py` 里会让别人 import router → 分组糊掉 |
| ⑤ | 到这一步前面全就绪，路由只做「接参 → 调 → 包信封」 |
| ⑥ | **契约 §7 #5 拍板：后端 `/auth/me` 跑通后再做前端**。倒过来的话，前端要拿一个不存在的接口反复试错 |
| ⑦ | `curl` 不能省：Cookie 往返（`Set-Cookie` → 浏览器带 `Cookie`）是**真实 HTTP 栈行为**，TestClient 测不到 |

### D.2 开工前置（会卡住进度，先处理）

| # | 事项 | 为什么 |
|---|---|---|
| 1 | **先落一次 git 提交** | 工作区现有后端 5 文件 + 前端 5 文件**全未提交**。带着一堆未提交改动开工，出问题时分不清是谁引入的 |
| 2 | **极验账号**：注册极验拿 `captcha_id` + `captcha_key`；**先用官方公开测试账号跑通**（`captcha_id = 54088bb07d2df3c46b79f80300b0abbe`，`captcha_key = d23bf3f2507ec46e6abd6aadc4e320bb`） | 外部依赖。**但极验有测试账号 → 不阻塞开工**（同 M5 联盟账号"注册有周期"的教训，这次压力小） |
| 3 | `requirements.txt`：**加 `httpx`**（从 dev 提到运行依赖）；`Pillow` 标注"本轮不需要" | 极验版不需要 Pillow |
| 4 | `fast` 环境：**本轮不需要装 `redis`** | 见 §A.2 —— 限流归 M6，且极验自带防重放 |
| 5 | **确认 `.gitignore` 挡住 `.env`** | ⚠️ `.env` 已入库过 MySQL root 密码，这次要往里加厂商密钥 |
| 6 | 极验后台配**域名白名单**（本地开发要含 `127.0.0.1`） | 不在白名单里，前端弹窗会直接失败 |

### D.3 本轮验收标准

**第 1 轮已有**（保持）：注册 / 重复注册 / 大小写归一 / 非法邮箱弱密码 / 登录成功 / 错密码 / 防枚举 / 哈希非明文。

**本轮新增**：

- [ ] 登录响应带 `Set-Cookie: session=…; HttpOnly; SameSite=Lax; Path=/`
- [ ] 库里 `user_sessions.token_hash` 是 64 位 hex，**不等于** Cookie 里的原文（泄库≠可盗用）
- [ ] 带 Cookie 访问 `/auth/me` → `200`，`data` 含 `id / user_email / username / create_time`，**物理上不含** `password_hash`
- [ ] 不带 Cookie / 乱造 Cookie → `401 / 40101`
- [ ] 登出 → `200`；**登出后再访问 `/auth/me` → `401`**；**不带 Cookie 登出也返回 `200`**（幂等）
- [ ] **传假 `lot_number` → `400 / 40002`，且不产生任何 DB 读写**（证明"验证码在查库之前"）
- [ ] 验证码不过 + 密码也错 → 返回 **`40002` 而不是 `40102`**（同上，证明顺序）
- [ ] `create_time` 在响应里带 `Z`，前端 `new Date()` 解析后不差 8 小时（证明没被当本地时间）

---

### 附：第 1 轮清单（已完成 · 存档）

1. `core/errors.py` 信封 + 业务码 + 异常处理器 ✅
2. `core/security.py` 哈希 + 密码策略 ✅
3. `models/user.py` 重写 ✅（实际 **6 字段**，不是 5——9-21 加回 `username`）
4. 建 `users` 表 ✅（用 **Alembic 迁移**，不是 `create_all`）
5. `services/auth_service.py` ⬜ **没建** —— 业务逻辑直接写在 `router/log_in.py` + `repository/auth_repo.py`
6. `router/*` + `main.py` 挂载 ✅
7. 自测 + 真实环境验证 ✅
8. ~~建 `user_sessions` 表~~ → **本轮做**
9. ~~`GET /auth/captcha` + 登录验证码 + Redis 限流~~ → **已作废**（换极验，见 §A）
10. ~~`get_current_user` + 滑动续期 + 登出~~ → **本轮做**
11. ~~补 `last_login_time` 写入~~ ✅ **09-24 已完成**并实测（`func.utc_timestamp()` 显式写）
12. 前端 Vite 代理联调 ✅ **已配**（代理 `/auth`）；**用 `/auth/me` 恢复登录态** → 本轮做

---

*本文件为「设计先行」产物。**与实现不一致时以 `docs/用户注册与登录-接口契约.md` §7.3 的处理口径为准**（默认跟代码走、由小宋回写；撞拍板或撞安全红线才先问）。*
